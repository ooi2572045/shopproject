services:
  - type: web
    name: shopproject
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn shopproject.wsgi:application --bind 0.0.0.0:$PORT
    preDeployCommand: python manage.py migrate && python manage.py collectstatic --noinput